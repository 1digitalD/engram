# AGENTS.md - Engram v4

This file is the entry point for agents working in this repository. Read it before touching code.

## Project Purpose

Engram is a self-hosted personal workspace: capture anything, recall it later, and run projects and tasks with AI assistance. It is single-user and self-hosted.

The active implementation is Engram v4.

## Active Documentation

| Document | Purpose |
|---|---|
| `docs/v6/IMPLEMENTATION_PLAN.md` | **Active implementation plan** (v6 Phases 0–6) |
| `docs/v6/SOLUTION_DESIGN.md` | v6 architecture, schema changes, trust policy |
| `docs/v6/TEST_PLAN.md` | v6 use cases, test cases, edge cases, metrics |
| `docs/ux-vision/UX_VISION.md` | Product vision; §10 = adopted build stance |
| `docs/V4_PRINCIPLES.md` | Non-negotiable product and architecture rules (still binding) |
| `docs/SCHEMA.sql` | Canonical v4 Postgres schema |
| `mcp_server/README_V4.md` | v4 MCP contract and tool surface |
| `docs/DEPLOY.md` | Local launchd + Tailscale deployment workflow (API `:5001`, MCP `:8765`) |
| `EXECUTION-TRACKER.md` | Fresh-agent handoff and current phase status |

Everything under `docs/archive/` and `docs/iterations/archive/` is history —
do not treat it as guidance. `prd.json` at repo root is the Loopsmith overlay
slot for whichever slice is being drained.

## Non-Negotiable Rules

- `/api/v4` is the only target runtime API.
- Do not build compatibility adapters for old response shapes.
- Do not store relationship IDs inside `properties`.
- All relationships must use `EntityLink` / relationship records.
- Notes remain source artifacts. AI may extract from notes, never convert them.
- Safe metadata and high-confidence linking may be auto-applied with audit events.
  (v6 Phase 1 narrows this: entity *creation* is never auto-applied, and writes to
  pinned fields demote to proposals — see `docs/v6/SOLUTION_DESIGN.md` §7.)
- Risky creation, status, deletion, merge, and relationship-deletion work must be suggestions.
- MCP is write-enabled for v4 and must stay aligned with the active `/api/v4` contract.
- Work proceeds slice by slice (see `docs/v6/IMPLEMENTATION_PLAN.md`) with validation before moving on.
- UI: do not extend `ui/src/views/` or `ui/src/lab/` (deleted in v6 Phase 6);
  new UI work goes in `ui/src/next/`.

## ⚠️ Production Data Safety (read before any schema or deploy work)

The production DB at `postgresql://engram:engram@localhost:5432/engram` contains real data.

- **Never run `flask init-db` against production** — it wipes all data.
- **Never set `DATABASE_URL` or `TEST_DATABASE_URL` to port 5432** in test contexts.
- **Always run `bash scripts/backup_prod.sh` before any schema change or deploy.**
- Schema changes must be additive-only, shipped as numbered scripts in `scripts/migrations/`.
- Tests run only against the isolated test DB (port 5433, `docker-compose.test.yml`).
- See `docs/V4_PRINCIPLES.md` → "Production Data Safety" for the full protocol.

## Working Rules

- Read the v4 principles and implementation plan before changing code.
- Keep changes simple, explicit, and testable.
- Write or update tests for behavior changes.
- Run focused validation first, then broader validation.
- Commit logical, reviewable units.
- Do not touch `.venv/`, `venv/`, `ui/node_modules/`, or ignored build artifacts.
- Do not revert unrelated user changes.

## Validation Commands

```bash
# Backend tests — always use TEST_DATABASE_URL pointing to port 5433
TEST_DATABASE_URL=postgresql://engram:engram@localhost:5433/engram_test ./venv/bin/pytest -q
TEST_DATABASE_URL=postgresql://engram:engram@localhost:5433/engram_test ./venv/bin/pytest tests/unit/ -q
TEST_DATABASE_URL=postgresql://engram:engram@localhost:5433/engram_test ./venv/bin/pytest tests/integration/ -q

# Run backend pytest commands serially against the shared test DB.
# Do not run multiple pytest processes in parallel unless each process has its own isolated database.

# Frontend
cd ui && npm test
cd ui && npm run build

# Schema migration (additive only — never init-db on prod)
# Apply to test DB first: psql postgresql://engram:engram@localhost:5433/engram_test < scripts/migrations/NNN_name.sql
# Then prod (after backup): psql postgresql://engram:engram@localhost:5432/engram < scripts/migrations/NNN_name.sql

# Backup prod before any deploy
bash scripts/backup_prod.sh
```

## Conventions

- Backend: Flask + SQLAlchemy 2.x.
- Database: Postgres + pgvector using `docs/SCHEMA.sql`.
- **Never run `flask --app app.py init-db` against the production database** — it wipes all data.
- Frontend: React + Vite.
- Service layer owns business logic; API handlers should stay thin.
- Meaningful mutations write `entity_events`.
- AI actions use explicit `agent:*` actors and must not silently perform risky changes.

<!-- headroom:learn:start -->
## Headroom Learned Patterns
*Auto-generated by `headroom learn` on 2026-07-08 — do not edit manually*

### File reading — no slicing loops
*~3,763,658 tokens/session saved*
- The read loop is the single biggest waste (~3.7M tokens measured across sessions): the same large files (`api/v4_entities.py` ~59KB, `AGENTS.md`, `EXECUTION-TRACKER.md`, `docs/V4_WORLD_MODEL_PLAN.md`, `tests/integration/test_v4_capture_extraction.py`, `V5ReviewSheet.test.jsx`) are re-read in FULL many times per session. Read each guidance/plan doc ONCE at task start and work from that copy; for `api/v4_entities.py` and 50KB+ test files, `command grep -n "<pattern>"` first and Read only the needed line range, never the whole file, and never a second time.
- Do NOT repeatedly slice the same file with `sed -n`/`nl -ba`/python3 heredocs to locate sections — `ui/src/views/V5ThreadDetail.jsx`, `V5ThreadDetail.test.jsx`, and `v5ThreadDetailUtils.js` are all <15KB; read once in full and work from that copy.
- When an `apply_patch`/Edit fails to match, re-read the exact target region once, then patch — do not blind-retry (one session issued 39 apply_patch calls to the same file).

### Python / pytest
*~130,000 tokens/session saved*
- The ONLY working interpreter is `/Volumes/lex1t/dev/shared/repos/engram/venv/bin/python` (symlinked; `.venv`, `.venv312`, `.test-venv` are broken or permission-denied; bare `python` does not exist — exit 127; bare `python3` lacks flask/psycopg2/pytest-repo-deps).
- NEVER try to create a new venv in a worktree: homebrew `python3.12/3.14 -m venv` fails on `ensurepip` every time (broken installations), and `uv` is not installed. Sessions burned 10+ calls each on this. Use the absolute main-repo pytest path on the FIRST try: `TEST_DATABASE_URL=postgresql://engram:engram@localhost:5433/engram_test PYTHONPATH=. /Volumes/lex1t/dev/shared/repos/engram/venv/bin/pytest <path> -q` — `./venv/bin/pytest` does NOT exist in worktrees; do not probe for it.
- Semantic/hybrid search tests need an API key present: prefix with `OPENAI_API_KEY=dummy` (mocked tests only need the var set). Do NOT paste the real key from `.env` into commands.
- Run each targeted pytest file ONCE after a batch of edits, not after every edit — the same pytest command was re-run up to 98x in one session (~50K tokens). Batch: edit everything, then one focused run, then one full run at the end.
- Known pre-existing failures unrelated to most changes: `tests/integration/test_v4_search.py::test_semantic_search_with_mocked_embeddings` and `test_hybrid_search_uses_rrf`. Don't loop re-running/stashing to bisect them.

### Worktrees
*~60,000 tokens/session saved*
- Codloop worktrees (`/Volumes/lex1t/dev/shared/repos/.engram-codloop-worktrees/*`) have NO `venv/` and NO `ui/node_modules/` — this is expected; do not spend calls probing with `ls`/`find`/`which`. Backend: absolute pytest path (see Python section). UI: `ln -s /Volumes/lex1t/dev/shared/repos/engram/ui/node_modules ui/node_modules`, run tests/build, then `rm ui/node_modules` BEFORE `git add -A` (the symlink is not gitignored and will be committed otherwise — this happened and needed a revert commit).
- `scripts/v6_validate_slice.sh` and `EXTRA_PYTEST=... bash scripts/v6_validate_slice.sh` hard-code `./venv/bin/pytest`; in a worktree first run `ln -s /Volumes/lex1t/dev/shared/repos/engram/venv venv`, run the script, then `rm venv` before committing. Same for `.env`: copy/symlink from the main repo if the script needs OPENAI_API_KEY, and remove it before commit.
- Agent worktrees under `.claude/worktrees/` are often checked out at an old commit and may be missing newer docs (e.g. `docs/iterations/*`). First step in any worktree: `command git log --oneline -3` vs main, then `command git merge --ff-only main` (or rebase) before feature work; read missing docs from the main repo path `/Volumes/lex1t/dev/shared/repos/engram/...`.

### UI tests (Vitest)
*~50,000 tokens/session saved*
- Always `cd <worktree>/ui` (or use the ui/ package dir) before `npm test` — running from the worktree root fails with ENOENT package.json.
- Run the full targeted test file ONCE after a batch of edits — `npm test` ran 15x and `npm test -- V5ReviewSheet` 12x in single sessions (~44K tokens combined). Batch UI verification (`npm test`, `npm run build`, `npm run lint`) into one final pass instead of after each edit.
- If `npm test` fails with missing vite/vitest in a worktree, symlink node_modules from the main repo (see Worktrees) — `npm install`/`npm ci` in the worktree also works but takes longer; never debug PATH.

### Test database (Docker)
*~49,391 tokens/session saved*
- Test Postgres runs in container `engram-postgres_test-1` on host port 5433; dev DB is `engram-postgres-1` on 5432. If it's down: `docker compose -f docker-compose.test.yml up -d`.
- `pg_isready` is NOT on PATH (fails exit 127 — hit in 8+ sessions). Check readiness with `docker exec engram-postgres_test-1 pg_isready -U engram -d engram_test` on the first try.
- Bare `psql` is NOT on PATH. Use `/opt/homebrew/opt/libpq/bin/psql postgresql://engram:engram@localhost:<port>/<db> -c "..."`, or docker exec: `docker exec -e PGPASSWORD=engram engram-postgres_test-1 psql -h 127.0.0.1 -U engram -d engram_test -c "..."`. Batch related queries into one call.
- NEVER try to boot a local homebrew Postgres (`postgresql@14`/`@16` initdb/pg_ctl, `/tmp/pg14.log` tail loops) — one session burned ~30 calls symlinking share dirs and copying extensions. The Docker containers are the only supported DBs.

### Computer-use / browser verification
*~98,119 tokens/session saved*
- `get_app_state` (computer-use) returns ~85–160KB of accessibility tree PER CALL, and every `click`/`set_value`/`type_text` already returns the updated state — NEVER call `get_app_state` again after an interaction to "check" the result.
- Prefer API-level smoke checks (`curl -s http://127.0.0.1:5001/api/v4/...` piped to a python3 json summary) over driving Firefox; use the browser only when visual behavior itself is the question.

### Git hygiene in sessions
*~4,300 tokens/session saved*
- Run `git status --short` at most once per logical step (before staging, after commit) — one session ran it 40 times. Combine `git status --short && git diff --stat` into a single call when checking work state.
- Combine final verification (`npm test`, `npm run build`, `git diff --check`) into one batch at the end instead of re-running each after every edit (`npm run build` ran 7x, `npm test` 7x in one session).

### Interactive session polling (write_stdin)
*~1,500 tokens/session saved*
- When polling a long-running command via `write_stdin` with empty chars, use `yield_time_ms: 5000–10000` on the first poll instead of repeated 1000ms polls — sessions issued 3–7 back-to-back empty polls per command (10+ separate poll loops observed). One longer wait replaces the whole loop.

<!-- headroom:learn:end -->
