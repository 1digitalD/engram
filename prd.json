{
  "iteration": "v4-noise-reduction-tier1",
  "goal": "Reduce low-signal noise surfaced by the v4 workspace so attention lands on items that need a decision today. Tier 1 of 2 from the diagnostic at /Volumes/lex1t/OC-workspace/memory/engram-cleanup-diagnostic.md.",
  "riskLevel": "low",
  "userProblem": "Today view and other entity list views render entities with null or placeholder titles as the literal string 'Untitled'. This produces confusing rows the user cannot act on. Separately, V4Today renders 8 sections of equal weight on first paint, so 12 idle projects and 25 recent notes visually bury the 3 actually-due follow-ups. Result: high cognitive load, missed follow-ups.",
  "whyNow": "User reported too much noise to focus on important work. Diagnostic identified these two surfaces as the highest-leverage low-risk fixes. Task 1 (Untitled fallbacks) already shipped in commit af3dd806 on main. Remaining work is task 2 (V4Today collapse) and task 3 (React-side regression test that complements the pytest regression landed with task 1).",
  "scope": {
    "filesAffected": [
      "ui/src/views/V4Today.jsx",
      "ui/src/views/V4Today.module.css",
      "ui/src/views/V4Today.test.jsx",
      "ui/src/views/__tests__/untitled-render.test.jsx (new)"
    ],
    "routesAffected": [],
    "dataOrApiDependencies": [],
    "writePathsAffected": []
  },
  "tasks": [
    {
      "id": "untitled-fallbacks-mcp-and-ui",
      "title": "Drop 'Untitled' fallback strings in MCP and UI",
      "description": "ALREADY COMPLETED. Replaced `entity.title || 'Untitled'` style fallbacks in mcp_server/v4_formatters.py (13 sites) and 7 UI view components with `(no title)` plus entity id and type via a shared entityTitleLabel() helper in ui/src/utils/entityDisplay.js. Added tests/unit/test_mcp_v4_formatters.py with 3 regression tests asserting 'Untitled' is not in formatter output. All 6 pytest tests pass, npm run build passes, grep returns zero matches.",
      "risk": "low",
      "acceptanceCriteria": [
        "`grep -rn \"'Untitled'\\|\\\"Untitled\\\"\" mcp_server/ ui/src/` returns no matches",
        "MCP formatters emit `(no title)` with id and type when title is null",
        "React list rows render entity id and type placeholder when title is null",
        "PYTHONPATH=. ./venv/bin/pytest -q tests/unit/test_mcp_v4_formatters.py passes (6/6)",
        "cd ui && npm run build passes"
      ],
      "validationCommands": [
        "bash -c \"! grep -rn \\\"'Untitled'\\|\\\\\\\"Untitled\\\\\\\"\\\" mcp_server/ ui/src/\"",
        "cd /Volumes/lex1t/dev/shared/repos/engram && PYTHONPATH=. ./venv/bin/pytest -q tests/unit/test_mcp_v4_formatters.py",
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm run build"
      ],
      "blocked": false,
      "passes": true,
      "attempts": 1,
      "commit": "af3dd806"
    },
    {
      "id": "v4today-default-collapse-low-signal",
      "title": "Default-collapse low-signal sections in V4Today",
      "description": "V4Today currently renders 8 sections on first paint. After this change, the four low-signal sections \u2014 Projects without open tasks, Stale projects, Suggested archival, Recent notes \u2014 are collapsed by default and reveal via a 'Show N more' disclosure. The remaining four \u2014 Focus now, Overdue, Due today/follow-up, Stuck \u2014 remain visible at top. Cap the Projects without open tasks list at 5 visible items (with 'View all 12' link if more exist). Recent notes stays in the same screen but is collapsed, not removed. This is presentation only \u2014 the underlying `/api/v4/today` payload is unchanged. Keyboard accessibility required: collapse controls must be focusable buttons (not divs), and aria-expanded must reflect state.",
      "risk": "low",
      "acceptanceCriteria": [
        "On first render of V4Today with mixed-state data, Projects without open tasks / Stale projects / Suggested archival / Recent notes are collapsed (not visible in DOM or hidden via aria-hidden)",
        "Each collapsed section shows a summary line ('12 idle projects \u2014 show') on a focusable button and expands on click/Enter/Space",
        "Projects without open tasks renders at most 5 items by default with a 'View all N' link that expands the list",
        "Focus now, Overdue, Due today/follow-up, and Stuck sections render fully as before",
        "ui/src/views/V4Today.test.jsx adds a test that confirms collapsed initial state for the four low-signal sections",
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm run build passes",
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm test -- --run src/views/V4Today.test.jsx passes"
      ],
      "validationCommands": [
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm run build",
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm test -- --run src/views/V4Today.test.jsx 2>&1 | tail -30"
      ],
      "blocked": false,
      "passes": true,
      "attempts": 1,
      "commit": "2f41a0e3"
    },
    {
      "id": "untitled-regression-test-react",
      "title": "Add React-side regression test that 'Untitled' string does not appear in rendered DOM",
      "description": "Task 1 already added Python-side regression coverage in tests/unit/test_mcp_v4_formatters.py. Add the parallel React coverage: a Vitest test that renders a fixture list with at least one null-title entity through V4EntityList (or any view using entityTitleLabel) and asserts the literal string 'Untitled' is not in the rendered DOM. Also assert the placeholder includes the entity id and type so the disambiguation contract is enforced on the UI side too. Test file: ui/src/views/__tests__/untitled-render.test.jsx.",
      "risk": "low",
      "acceptanceCriteria": [
        "ui/src/views/__tests__/untitled-render.test.jsx exists",
        "Test renders V4EntityList with a fixture containing an entity with title: null",
        "Assertion: 'Untitled' is not in the rendered text",
        "Assertion: '(no title)' is in the rendered text",
        "Assertion: the entity id is in the rendered text",
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm test -- --run src/views/__tests__/untitled-render.test.jsx passes",
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm run build passes"
      ],
      "validationCommands": [
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm test -- --run src/views/__tests__/untitled-render.test.jsx 2>&1 | tail -30",
        "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm run build"
      ],
      "blocked": false,
      "passes": false,
      "attempts": 0
    }
  ],
  "validationCommands": [
    "cd /Volumes/lex1t/dev/shared/repos/engram && PYTHONPATH=. ./venv/bin/pytest -q",
    "cd /Volumes/lex1t/dev/shared/repos/engram/ui && npm run build"
  ],
  "codingLoopPolicy": {
    "primaryExecutor": "opencode",
    "fallbackExecutors": [
      "cursor",
      "codex",
      "claude"
    ],
    "platform": "openclaw",
    "allowExecutorFallback": true,
    "requireHumanForDependencies": true,
    "executorLaunchTimeoutSeconds": 1800
  },
  "publication": {
    "branchPrefix": "loopsmith",
    "pushEnabled": false,
    "prEnabled": false
  },
  "nonGoals": [
    "Schema changes or migrations",
    "AI extraction prompt rewrites (already shipped in 1a21ed3b)",
    "Adding NOT NULL constraints on Entity.title",
    "Removing Entity.ai_meta duplicates (Tier 2 deferred per diagnostic)",
    "Adding a 'Focused mode' toggle (Tier 2 deferred)",
    "Moving Recent notes out of Today view entirely (deferred \u2014 collapses instead)",
    "Cron job for slipped-bug DMs (user opted out of cron in earlier conversation)"
  ],
  "continuityNote": {
    "keyDecisions": [
      "Already-shipped work (commit 1a21ed3b and af3dd806) handles the extraction-prompt and Untitled fallback concerns. Do not re-touch.",
      "Empty-content activity update guard already exists at api/v4_entities.py:1754 and :3258. Do not re-touch.",
      "Validator commands run from the main repo path (/Volumes/lex1t/dev/shared/repos/engram) because worktrees do not carry the gitignored venv/ directory.",
      "npm install is NOT in any validator \u2014 vite and dependencies are cached; install only adds latency. If a missing-dependency error appears, install in the main repo once and retry.",
      "executorLaunchTimeoutSeconds is set to 1800 to absorb first-time build/indexing work; the default 300 was insufficient for npm build after a fresh worktree.",
      "Render null titles as '(no title) [type-id]' so user retains disambiguation context (decided in task 1).",
      "Cap idle projects at 5 visible, do not remove the section \u2014 user may still want to scan it.",
      "Keep Recent notes collapsed in place rather than relocating to Inbox (out of scope per non-goals)."
    ],
    "filesLikelyToChange": [
      "ui/src/views/V4Today.jsx",
      "ui/src/views/V4Today.module.css",
      "ui/src/views/V4Today.test.jsx",
      "ui/src/views/__tests__/untitled-render.test.jsx (new)"
    ],
    "mainRisks": [
      "Existing test snapshots that include collapsed-section state may need updating",
      "Keyboard accessibility on collapse controls \u2014 must be <button> not <div onClick>",
      "V4Today is the most-used view; visual regressions hit user immediately"
    ],
    "nextStepIfInterrupted": "Tasks 2 and 3 are independent \u2014 V4Today collapse and React regression test can land on separate branches. Either can be retried on its own."
  }
}
